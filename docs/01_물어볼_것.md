# 물어볼 것

**막혀 있는 것을 누가 풀어줘야 하는지의 목록이다.** 각 항목의 자세한 근거는
해당 API 문서에 있고 여기에는 **무엇이 막히는지**와 **무엇을 받아야 하는지**만 적는다.

답을 받으면 이 문서가 아니라 **해당 API 문서를 먼저 고치고**, 여기서는 줄을 지운다.
`00_문서_인덱스.md` 「앱 개발을 막고 있는 것」과 짝이다 — 그쪽이 우리 사정이고 이쪽이 요청이다.

마지막 정리 2026-09-09 (서버 회신 다섯 건 반영).

---

## 0. 인증 — 거의 풀렸다 (2026-09-01 서버 답)

서버가 「HRM 로그인 붙이기」로 답을 줬고, **우리가 물어본 다섯 중 넷이 없어졌다.**

**방식이 우리가 생각한 것과 달랐다.** 프런트가 Keycloak과 직접 말하는 것이 아니라
**서버가 로그인을 중계하고 프런트에는 토큰만 넘긴다.** 그래서 —

| 물어봤던 것 | 답 |
|---|---|
| 프런트용 public client 등록 | **필요 없다.** 서버가 Keycloak과 직접 로그인한다. PKCE도 OIDC 라이브러리도 필요 없다 |
| 관리팀 콜백 등록 | **이미 돼 있다.** `/auth/callback`을 `localhost` 5173·3000과 `127.0.0.1` 네 조합으로 열어 뒀다 |
| 커스텀 스킴 리다이렉트가 되는지 | **된다. 등록만 하면 된다** — 아래 남은 것 참고 |
| 테스트 계정 Keycloak 아이디 | 우리가 가입하고 알려주면 된다 — 아래 남은 것 참고 |

**우리 코드는 거의 맞아 있었다.** fragment(`#token=`)로 받는 것, `sessionStorage`에 두고
`localStorage`를 피하는 것, `Authorization: Bearer`, `/auth/me`로 권한을 받는 것,
401 재이동을 한 번으로 막는 것까지 전부 서버 안내와 같다.

**고친 것은 둘이다** (2026-09-01).

- 로그인 입구를 `oauth2/authorization/keycloak` → **`/auth/login`**으로 바꿨다.
  둘 다 살아 있지만 `redirect`를 받는 쪽은 `/auth/login`뿐이다
- **모바일이 `redirect`를 안 싣고 있었다.** `callbackUrl()`을 만들어 두고 `loginUrl()`에
  붙이지 않아서, 등록이 끝나도 **토큰이 앱으로 오지 않을 코드였다.** 진짜 버그였다

### 실제로 끝까지 돌려봤다 (2026-09-01)

**앱 쪽 배선은 확인됐다.** 로그인 버튼 → Keycloak 로그인 → OTP → 서버가
`localhost:5173/auth/callback` 으로 정확히 돌려보내는 데까지 실제로 통과했다.

거기서 **토큰 대신 `#error=` 가 왔다.** 화면에 뜬 문구는 우리가 만든 것이 아니라
서버가 보낸 값을 그대로 보여준 것이다 — `로그인하지 못했습니다. 관리자에게 문의하세요.`

| 단계 | 결과 |
|---|---|
| 회원가입 · 비밀번호 | **통과** |
| OTP 등록 (Keycloak이 필수로 걸어 뒀다) | **통과** |
| 콜백 도달 (`#error=` 를 우리 화면이 받아 그림) | **통과** |
| 그룹 배정 · 인사 정보 연결 | **여기서 막혔다** |

**어느 쪽인지는 응답으로 구분되지 않는다.** 서버 문서가 그렇게 적고 있고 이유는 서버
로그에만 남는다 — 아이디를 알려주면 서버가 어디서 끊겼는지 봐 준다.

**OTP 등록이 필수라는 것은 문서에 없던 것이다.** 뒤에 가입할 사람도 같은 화면을 만난다.
앱 항목이 여러 개 쌓이면 옛 코드를 넣게 되므로, 다시 등록할 때는 인증기 앱의 옛 항목을
지우고 새 QR로 받는다.

### 그 뒤 — 로그인이 열렸다 (2026-09-03)

**그룹 배정이 풀려 관리팀 웹으로 로그인이 된다.** `sehuii`로 토큰을 받아 `/auth/me`가
통했고 직원 46명이 실제 값으로 떴다. 9-01에 `#error=`로 막히던 자리가 지나갔다.
자세한 것은 `00_문서_인덱스.md` 「로그인이 열렸다」에 있다.

### 콜백 주소가 등록됐다 (2026-09-04)

**0.3이 해소됐다.** 서버가 `hr://auth/callback`을 등록했고 부딪혀 확인했다.

| 부른 주소 | 응답 |
|---|---|
| `hr://auth/callback` | **302** → `/oauth2/authorization/keycloak` |
| `hr://auth/nope` (대조군) | `400 등록되지 않은 콜백 주소입니다` |

대조군을 같이 부른 것은 **검증이 꺼진 것이 아니라 우리 주소만 추가된 것**인지 보기
위해서다. 엉뚱한 주소는 여전히 400이다.

**확인된 것은 입구뿐이다.** 로그인을 통과한 뒤 서버가 실제로
`hr://auth/callback#token=<JWT>`로 돌려보내는지는 **앱이 기기에 있어야** 볼 수 있다.
커스텀 스킴이라 PC 브라우저로는 끝까지 가지 못한다.

### 앱이 슬래시 3개를 보내고 있었다 (2026-09-08) — 고쳤다

**등록은 맞았는데 앱이 다른 주소를 보내고 있었다.** 배포 APK를 안드로이드 실기기에
올려 로그인을 눌렀더니 서버가 이렇게 답했다.

```
등록되지 않은 콜백 주소입니다. 백엔드에 이 주소를 알려주세요: hr:///auth/callback
```

`expo-linking`의 `createURL`이 마지막을 `scheme + ':' + '/' + hostUri + path`로 조립하는데,
커스텀 스킴 배포 빌드는 `hostUri`가 비어서 `ensureLeadingSlash('', true)`가 `/`를 돌려주고
거기에 경로의 앞 슬래시가 또 붙는다. **개발 빌드에서는 `hostUri`가 `192.168.x.x:8081`이라
드러나지 않는다** — 브라우저로도 개발 빌드로도 못 잡고, **배포 APK를 실기기에 올려야
보이는 버그였다.** 이 확인을 한 이유 그대로다.

경로에서 앞 슬래시를 뺐다 (`src/lib/auth.ts`, PR #119). 개발 빌드는 그대로다.

**지금은 둘 다 등록돼 있다** (2026-09-08 실호출 확인).

| 부른 주소 | 응답 |
|---|---|
| `hr://auth/callback` (고친 앱이 보내는 것) | **302** |
| `hr:///auth/callback` (지금 퍼진 APK가 보내는 것) | **302** |
| `hr://auth/nope` (대조군) | `400 등록되지 않은 콜백 주소입니다` |

**서버에 더 요청할 것은 없다.** 남은 것은 새 APK로 로그인이 끝까지 가는지 보는 것뿐이다.

### 갱신·로그아웃이 생겼다 (2026-09-08 서버 답)

**토큰 수명이 5분에서 15분이 됐고, 갱신과 로그아웃 경로가 열렸다.** 명세에도 올라와 있다.

| 무엇 | 규칙 |
|---|---|
| `GET /auth/refresh` | **세션 쿠키로 인증한다** — `credentials: include` 필수, **`Authorization` 헤더를 붙이지 않는다**(만료된 토큰으로는 신원을 증명할 수 없다). 200 `{token, expiresIn}` · 401 이면 재로그인. **만료 1분 전에 미리 부른다** |
| `POST /auth/logout` | 서버 세션만 끊는다. **이미 발급된 토큰은 JWT 라 거둬들일 수 없다 — 프런트가 지워야 로그아웃이 완성된다.** 이미 로그아웃 상태여도 200. **Keycloak 세션은 안 끊긴다** |
| 수명 15분 | **`expiresIn` 을 쓰고 15분을 코드에 적지 않는다.** Keycloak 설정이라 또 바뀔 수 있다 |
| 401 | **정상 경로다.** 서버가 인증 정보를 메모리에 들고 있어 **배포할 때마다 날아간다** — 배포 직후 `refresh` 는 401 이다. 오류 화면이 아니라 재로그인으로 이어져야 한다 |

**쿠키가 `SameSite=Lax` 라 프런트와 API 가 같은 사이트(`*.dvi-ind.com`)여야 실린다.**
개발은 `localhost` 라 다른 사이트여서 **쿠키가 실리지 않는다** — 개발 중 갱신 401 은 정상이고,
실제로 도는지는 **배포 주소가 정해져야** 확인된다. 다른 도메인에 올릴 거면 서버에 알려야 한다
(쿠키 설정을 바꿔야 한다고 했다).

**관리팀 웹에 붙였다** (2026-09-09) — 만료 1분 전 갱신, 로그아웃에서 서버 세션까지 끊기.
갱신이 실패해도 로그아웃시키지 않는다. 지금 토큰은 아직 살아 있고, 실제로 죽는 순간 401 이
와서 기존 재로그인 경로가 받는다. **쿠키가 안 실리는 개발 환경에서 튕기지 않게 하려는 것도 있다.**

#### 콜백 주소 — 슬래시 3개짜리가 제거됐다

| 주소 | |
|---|---|
| `hr://auth/callback` | **✅ 앱용** (슬래시 2개) |
| `hr:///auth/callback` | **❌ 제거됐다** |

**그래서 PR #119 이전 APK 는 이제 로그인이 400 이다.** 실기기 확인은 `9c83030a` 이후 빌드로만
된다. 웹 배포 주소가 정해지면 미리 알려야 등록된다 — 슬래시 하나만 달라도 400 이고
서버가 느슨하게 받아주지 않는다(등록 안 된 주소로 토큰이 나가는 것을 막으려고).

#### 모바일은 방식이 뒤집혔다 — 답 셋이 더 필요하다

서버 안내: **앱은 `/auth/login` 을 거치지 않고 Keycloak 과 직접 OIDC + PKCE 로 붙는다**
(RFC 8252). 앱이 리프레시 토큰을 직접 들고 조용히 갱신해 브라우저가 반복해서 뜨지 않는다.
`react-native-app-auth` 를 권했고, `/auth/refresh`·`/auth/logout` 은 **웹 전용**이라고 했다.

**2026-09-01 답(「프런트용 public client 도 PKCE 도 필요 없다, 서버가 중계한다」)이 뒤집힌 것이다.**
지금 앱은 서버 중계 방식으로 만들어져 있고 9-08에 실기기에서 토큰을 받아 `/auth/me` 까지
통과했다. **전환에 필요한 것이 문서에 없어서 코드를 쓰지 않았다 — 30·31·32번이다.**

### 남은 것 둘

| # | 무엇 | 누가 |
|---|---|---|
| 0.1 | **Keycloak 회원가입하고 아이디 알려주기** — `https://api.dvi-ind.com/hi-yo/auth/login`에서 Register. 아이디는 영문. 박진용 `jinyong3792`는 등록됐고, **박세희는 `sehuii`로 가입해 서버에 알렸다** (2026-09-01). 이유진·김민준 두 사람이 남았다 | **우리** |
| 0.2 | **SSO 팀에 `/engineering` 그룹 배정 요청** — 가입만으로는 토큰에 롤이 안 실린다. 백엔드가 못 하는 일이다. **박세희는 배정돼 로그인이 열렸다** (2026-09-03). 이유진·김민준이 남았다 | **우리** |

~~0.3 `hr://auth/callback` 등록~~ — **2026-09-04 해소됐다.** 위 「콜백 주소가 등록됐다」.

**남은 둘은 같은 사람 얘기다.** 이유진·김민준이 가입하고 그룹을 배정받아야 한다.

**가입한 아이디를 서버에 알려야 한다.** 서버가 `sso_username`에 넣어 준다.
**그것이 없으면 로그인은 되는데 API가 401이다** — 박세희가 9-01에 막혔던 지점이 여기고,
서버 쪽에서 넣어야 풀린다 (2026-09-02 회신).

권한 검증(16번)은 **서버가 막는다는 답을 받았다.** 그래도 일반 직원 계정은 여전히
필요하다 — 지금 계정은 관리팀이라 **본인용 화면이 실제로 어떻게 보이는지** 확인할 수 없다.

**스텁 헤더 `X-Debug-Employee-No`는 9-01자로 서버가 지웠다.** 그래서 로그인 없이는
모든 API가 `401`이다. 그 헤더가 없어도 관리팀으로 통과시키던 것이 우리가 보고한
「재직증명서 단건·PDF 권한 구멍」의 정체였고, **서버가 그렇게 확인해 줬다.**

우리가 줘야 하는 것 — **배포 주소**(CORS·콜백 등록용). 아직 미정이다. **갱신 쿠키가 `SameSite=Lax` 라 `*.dvi-ind.com` 하위가 아니면 토큰 갱신이 아예 안 된다** — 도메인이 다르면 서버가 쿠키 설정을 바꿔야 한다 (2026-09-08).
---

## 1. 서버 — 2026-09-02에 전부 답이 왔다

**22건 전부에 답을 받았다.** 회신 문서는 「기다리던 답」이다. 열여섯은 이미 배포됐고,
여덟은 코드를 열어 확인한 값으로 답이 왔다. 둘은 회사가 정해야 하고(3장 A·B),
둘은 우리 몫이다(0.1·0.2).

**아래는 요청 목록이 아니다.** 무엇이 열렸고 우리가 무엇을 해야 하는지의 목록이다.
다시 묻지 않는다.

**`API_*.md`는 아직 이 답들을 반영하지 않았다.** 이 문서의 규칙대로라면 API 문서를 먼저
고치고 여기서 줄을 지워야 하는데, 한 번에 22건이 와서 **먼저 여기에 다 적어 뒀다.**
API 문서 반영은 화면을 고치면서 같이 한다 — 특히 `API_신청결재.md` 7장은 결재가
2단계가 되면서 **틀린 내용이 됐다.**

### ★ 화면을 고쳐야 한다 — 결재가 2단계가 됐다

**회사 「휴가(근태)신청서」 결재란이 「작성 · 검토 · 승인」 세 칸이었다.** 작성은 신청인
자리이니 결재는 **검토와 승인 두 단계**다. 인사팀이 「원칙대로 가자」고 확정했다.

    POST /requests/{id}/review      ← 새로 생겼다
    POST /requests/{id}/decision

몸통은 둘 다 기존 `RequestDecisionRequest` 그대로다 — `approved` · `comment` ·
`signatureMethod` · `signatureImage`.

| status | 화면이 보여줄 것 |
|---|---|
| `PENDING` | **검토** 버튼 → `/review` |
| **`REVIEWED`** | **승인** 버튼 → `/decision`. 단 **내가 검토자면 비활성** |
| `APPROVED` · `REJECTED` · `CANCELED` | 결재 완료 |

**`REVIEWED`가 새로 생겼다.** status를 열거해 쓰는 곳을 빠짐없이 고친다 — 목록 필터 ·
뱃지 · 정렬 전부다.

**검토한 사람은 승인할 수 없다.** 같은 사람이 두 칸을 채우면 단계를 나눈 뜻이 없어
서버가 **422**로 막는다. 화면에서 미리 비활성으로 둔다 — 누르고 나서 거부당하게 두는
것은 화면의 잘못이다.

응답에 `reviewerId` · `reviewerName` · `reviewComment` · `reviewedAt` 넷이 늘었다.
기존 `approverId` 계열은 이제 **승인** 자리다.

이미 승인 · 반려된 신청은 **검토 칸이 비어 있다.** 실제로 검토한 사람이 없는데 이름을
넣으면 그것이 증빙으로 남아 소급해 채우지 않았다. 결재 대기 중이던 신청은 검토부터
다시 밟는다.

**9-01에 정리한 「2단계 결재가 없다」가 뒤집혔다.** 그때는 검토자·승인자가 독립 역할이
아니라 관리팀 내 지정이라고 정리했는데, 서식을 받고 나서 **단계 자체가 둘로 나뉘었다.**
`API_신청결재.md` 7장을 고쳐야 한다.

### 열렸다 — 이제 만들면 된다

| # | 무엇 | 경로 |
|---|---|---|
| 1 | 주민번호 전용 경로 | `PATCH /employees/{id}/resident-no`. 응답에 값은 없고 `residentNoRegistered`가 `true`로 바뀐다 |
| 2 | 부서 · 직무 되돌려 보내기 | 조회에 `departmentId` · `jobId`가 실린다 + `PATCH /employees/{id}/assignment` |
| 3 | 당직 교체 후보 | `GET /duty/schedules/{scheduleId}/swap-candidates` — **일반 직원도 부를 수 있다.** `employeeId`와 `name`만 온다 |
| 8 | 급여명세서 | `GET /payroll/{id}/pdf` · `POST /payroll/{id}/deliveries` · `GET /payroll/{id}/deliveries`. PDF는 **확정된 급여만** |
| 11 | 연차촉진 | `GET /leave/promotions/targets` · `POST /leave/promotions` · `POST /leave/promotions/{id}/plan` |
| 12 | 신청인 서명 | `POST /requests`가 `signatureMethod` · `signatureImage`를 받는다. 응답에 **`applicantSigned`** — 기존 `signed`는 결재자 서명이라 그대로다 |
| 12③ | 단체연차 서명 | `PATCH /requests/{id}/signature` — **「만들고 나서 서명」**으로 정해졌다 |
| 13 | 서명 크기 | **base64 128KB** (`131,072`자). 넘으면 400 |
| 14 | 서명 배경 | **투명 PNG 그대로** 두면 된다 |
| 18 | 근태 확정 되돌리기 | `POST /attendance/judge?confirm=false` — **원래 됐다.** 기간용 `judge-range`도 생겼다 |
| 21 | 반차 시각 | `RequestTypeResponse`에 `amStartTime` · `amEndTime` · `pmStartTime` · `pmEndTime` |
| 22 | 미확정 급여 | 서버가 목록과 단건 **둘 다** 막는다. **금액이 기기까지 오지 않는다** |
| 23 | 연차 자동 부여 | `POST /leave/grants/accrual`로 수동 실행만 된다. **스케줄러는 꺼 뒀다** — 3장 B가 정해져야 켠다 |
| — | 단체연차 삭제 | `DELETE /company-leaves/{id}` — **적용 전만.** 적용됐으면 거부한다 |

**11번 촉진은 우리가 준 조건 그대로 들어갔다** — 기준일 1월 1일, 1차 소멸 6개월 전,
2차 2개월 전, 계획서는 통보 후 10일 이내. 대상 조회 응답에 `remainingDays` ·
`confirmedRemainingDays` · `alreadyNotified` · `planDueOnIfSentToday`가 들어 있어
**화면에서 마감일을 다시 계산하지 않는다.**

**12③을 「만들고 나서 서명」으로 정한 이유**는 단체연차가 회사가 정한 휴무일이라,
전원의 서명을 기다리면 **한 사람이 안 하면 급여 마감이 막히기** 때문이다. 차감은 그대로
두고 서명만 나중에 채운다. 같은 경로가 **관리팀 대리 등록 건에도 쓰인다** — 대리 등록된
신청서가 영영 `applicantSigned=false`로 남던 것도 같이 풀린다.

#### 화면을 고치면서 같이 지울 것

| 무엇 | 왜 |
|---|---|
| 급여 목록에서 미확정을 거르던 코드 | 서버가 막는다 (22) |
| 근태 확인 대화상자의 「되돌리는 경로가 없다」 | 되돌릴 수 있다 (18) |
| 반차 시각 하드코딩 `09:00` · `13:00` · `18:00` | 종류 응답에서 온다 (21). **단 값은 아직 확정이 아니다 — 3장 A** |

#### 조심할 것

- **`PATCH /employees/{id}/assignment`의 `null`은 「비운다」다.** 「그대로 둔다」가
  아니다. 한쪽만 바꾸려면 나머지는 조회에서 받은 현재 값을 그대로 실어 보낸다
- **`PUT /employees/{id}/bank-account`는 셋을 전부 덮어쓴다.** 일부만 보내면 나머지가
  `null`로 지워진다. **세 값을 항상 함께 보낸다**
- **서명은 서버에서 선택이다.** 관리팀 대리 등록 경로가 있어 필수로 두지 않았다.
  종이 서식을 그대로 옮기는 화면이면 **화면에서 필수로 받는다**
- **`CARRY_DEDUCT`는 값이 음수다.** 화면에서 음수 입력을 막는다면 이 유형만 열어 둔다

### 답만 온 것 — 코드는 바뀌지 않았다

| # | 답 |
|---|---|
| 5 | `MONTHLY` · `REGULAR` · `SENIORITY`는 짚은 대로 맞다. **A-306 「신입휴가 수동 입력」은 `NEW_HIRE`다** — 규칙 밖에서 관리팀이 산정해 넣는 값이라 `MONTHLY`와 갈린다. `CARRY_DEDUCT`는 엑셀에서 넘어온 과거 초과사용분 전용이고 **평상시 쓰지 않는다** |
| 6 | `baseItemCode`는 **다른 항목의 금액을 기준으로 하는 항목**에만 넣는다. 장기요양(`CARE`)이 건강보험(`HEALTH`)을 기준으로 하는 하나뿐인 것이 우연이 아니다. 비우면 지급총액에 곱해져 **금액이 30배가 된다.** 등록 화면은 「다른 항목을 기준으로 함」 체크박스로 두고 체크했을 때만 항목을 고르게 한다 |
| 7 | `autoCalculate`는 「매달 다시 계산」, **`roundUnit`은 절사다**(반올림이 아니다 — 급여대장 13건 중 12건이 10원 절사로 맞았다). 보수월액이 연 1회 신고하는 고정액이라 국민연금 · 건강보험 · 장기요양은 `false`, 당월 지급총액 기준인 고용보험만 `true`. **등록 화면 기본값은 `false` · `10` · `null`** |
| 10 | **계좌는 아무것도 마스킹되지 않는다.** 은행명 · 계좌번호 · 예금주 셋 다 원문이다. 마스킹된 값을 되돌려 보내 급여가 엉뚱한 계좌로 갈 위험은 **없다.** 대신 원문이 그대로 내려오니 **화면에서 마스킹해 보여준다** — `packages/format`의 `maskAccountNo`로 **만들었다** (2026-09-07). **두 앱 다 가린다** — 관리팀 직원 상세도 2026-09-07에 같이 가렸다 (2장 5번). 남은 것은 **서버가 가려서 줄 수 있는지** 하나다 (24번) |
| 15 | `residentNoMasked`는 **`901231-1******`** — `CLAUDE.md`와 같다. 원본 형식이 틀리면 `******-*******`. 증명서 양식의 `951106-*******`과는 다르니, 출력에서 뒷자리를 통째로 가려야 하면 **출력 쪽에서 따로 처리한다** |
| 16 | **서버가 막는다.** `/leave/balance/{id}` · `/payroll/employees/{id}` 둘 다 본인 또는 관리팀만 통과한다. 재직증명서 · 근태 · 당직 교체 후보 · 명세서 PDF도 같은 검사가 걸려 있다 |
| 17 | `dayOfWeek`는 **한글 한 글자**다 — `"월"` … `"일"`. `SATURDAY`가 아니다. 근태표와 당직표 둘 다 같다 |
| 19 | **막히지 않고 0원으로 돌고 있었다.** 요율이 없는 해로 급여를 계산하면 경고도 오류도 없이 **4대보험이 0원인 채로 급여가 나갈 수 있었다.** 물어본 덕에 찾았고 고쳤다 — 이제 계산을 거부하고 몇 년도 요율이 없는지 메시지에 담는다 |

**13번은 물어본 것이 사고를 막았다.** 저장 컬럼이 `TEXT`(64KB)여서 128KB는커녕
**실측한 최악값 72KB도 들어가지 않았다.** 길게 그은 서명 하나가 신청을 통째로
실패시키는 상태였다. `MEDIUMTEXT`로 넓혔다.

### 답이 오지 않은 것 — 다시 물어야 한다

**2026-09-09에 넷이 풀렸다** — 24 · 25 · 26 · 29번. 아래는 남은 것이다.

**30·31·32는 한 덩어리다** — 모바일 인증을 Keycloak 직접 연결로 바꾸는 건이고, 셋 다
와야 코드를 쓴다 (2026-09-08 회신에서 생겼다). 20번은 **서버가 관리팀에 다시 물어
답을 기다리는 중**이라 여기 둔다.

| # | 무엇 | 왜 남았나 |
|---|---|---|
| 20 | **직군에 따라 지각 · 조퇴 · 외출을 다르게 공제하는가** | 회신에서 빠졌던 것이 맞다고 확인됐고(번호가 20부터 하나씩 밀려 있었다), **서버가 2026-09-09에 관리팀으로 다시 물었다.** 회사 근태 시트가 **관리직은 표시만, 생산직(시급)은 30분 단위로 공제**한다고 적고 있는데 그것이 맞는지를 확인 중이다 (`API_근태.md` 8장). **답이 올 때까지 화면 문구는 지금 상태로 둔다** — 서버가 그렇게 안내했다 |
| 30 | **앱이 Keycloak 토큰을 직접 받으면 우리 API 가 그 토큰을 받는가** | 2026-09-08 인증 회신에서 나왔다. 지금은 서버가 발급한 JWT 를 `Authorization: Bearer` 로 붙인다. 앱이 OIDC + PKCE 로 Keycloak 과 직접 붙으면 손에 쥐는 것은 **Keycloak 액세스 토큰**인데, `/auth/me` 를 비롯한 API 가 그것을 검증하는지 아니면 서버 JWT 로 바꾸는 경로가 따로 있는지가 안내에 없다. **이게 갈리면 앱 로그인을 처음부터 다시 만드는 것과 같다** — 답을 받기 전에는 전환 코드를 쓰지 않는다 |
| 31 | **Keycloak 공개 클라이언트 정보를 주세요** | 30번이 「받는다」이면 바로 필요하다 — issuer(discovery) 주소 · `clientId` · 스코프, 그리고 **`hr://auth/callback` 을 Keycloak 쪽 redirect URI 에도 등록**해야 한다(서버의 콜백 목록과는 별개다). 라이브러리도 하나 늘어난다(`react-native-app-auth` 또는 `expo-auth-session`) — 어느 쪽이 맞는지도 위 정보가 있어야 정한다 |
| 32 | **서버 중계 경로는 계속 사는가 · 계정은 어떻게 바꾸는가** | ① 앱이 쓰던 `/auth/login?redirect=hr://auth/callback` 이 언제까지 유지되는지. 지금 배포된 APK 가 그 경로로 돌고 있어서, 없어지는 시점을 알아야 전환을 급하게 할지 정한다. ② **로그아웃해도 Keycloak 세션이 남아** 다시 로그인하면 같은 계정으로 바로 들어온다. 관리팀 화면에 「다른 계정으로 로그인하기」가 있는데 **실제로는 계정이 바뀌지 않는다** — `end_session` 경로를 알려주면 그 버튼이 제 일을 한다 |
| 27 | **검토자는 「반려」도 못 하는가** | 결재가 2단계가 되면서 서버 설명이 「검토자는 **승인할 수 없다**」까지만 적고 있다. 반려도 막히는지는 안 적혀 있다. 지금 화면은 **승인만 비활성이고 반려는 눌리게** 뒀다 — 막히는 쪽이면 검토자가 반려를 눌렀다가 오류를 보게 된다 |
| 28 | **근태 「확정 해제」를 화면에 둘 것인가** | 18번 답으로 `POST /attendance/judge?confirm=false`가 원래 됐다는 것은 확인됐다. **그런데 화면에 「확정 해제」 버튼을 따로 둘지가 안 정해졌다** — 지금은 판정 재실행이 그 부수효과를 낸다. 관리팀이 「해제하려고」 재실행을 누르는 흐름이 맞는지, 아니면 버튼을 나눠야 하는지 |

### 서버가 아직 못 준 것 (기다린다)

| 무엇 | |
|---|---|
| 급여명세서 **이메일 · 문자 발송** | 범위 밖이다. PDF와 교부 기록까지다 |
| **명세서 서식 원본** | 못 받아서 지금 양식은 임시다. 받으면 바뀐다 |
| 연차촉진 **알림 발송** | 범위 밖이다. 「통보했다」는 기록만 남긴다 |
| **연차 발생 데이터** | 3장 B가 정해지면 그 규칙으로 한 번에 채운다. 지금 임의로 넣으면 나중에 다시 지워야 한다 |

### 받은 문서 둘

- `docs/frontend-422-catalog.md` — 422가 나는 업무 규칙을 전부 훑어 표로 정리한 것.
  화면이 서버 메시지를 그대로 보여줄지 자기 문구로 바꿀지 정할 수 있다
- `docs/frontend-leave-balance.md` — `LeaveBalanceResponse` 필드별 설명과 실제 계산식

### 덤 — 근태에 값이 찼다 (2026-09-02)

**`attendance_daily`가 0건이었다.** 정기 배치가 오늘·어제만 보는데 세콤에서 4년치가
뒤늦게 들어와 **아무도 판정한 적이 없었다.** `1,468일 · 37,269건`을 판정했다. 근태
현황·52시간 화면에 이제 값이 들어온다 — **전부 잠정**이라 `unconfirmedDays`가 크게
나오는 것이 정상이다. 관리팀이 보정한 뒤 확정하는 흐름이 맞다.

**야간조 근태가 틀리게 계산되고 있었다.** 세콤은 찍은 달력 날짜로 기록하는데 야간근무는
한 근무가 이틀에 걸친다. 그 둘을 같은 행 안에서 짝지어 **서로 다른 근무의 출근과 퇴근을
엮고** 있었다. 645행 7명이 영향을 받았고 적게도 많게도 틀렸다 — 어떤 날은 근무가 통째로
0분이었고, 어떤 날은 6시간 근무가 15.5시간으로 부풀었다. 고쳤다.

---

## 2. 기획

| # | 무엇 | 막히는 것 |
|---|---|---|
| 1 | ~~화면 상세 스펙~~ | **끝났다 (2026-09-01).** S-101·S-501·S-601을 만들었다. 남았던 S-101 통장정보 수정도 **만들었다 (2026-09-07)** |
| 2 | ~~보정 · 판정 재실행 · 세콤 수집의 자리~~ | **정해졌다 (2026-09-01). 새 화면을 만들었다** — `apps/admin` `/attendance/operations` |
| 3 | ~~A-502를 A-302와 나눌 것인가~~ | **나누지 않기로 했다 (2026-09-01).** 회사 양식 「휴가(근태)신청서」가 연차·반차와 조퇴·무급휴무·예비군을 한 장에 담고 있다 — 원래 한 절차다 |
| 4 | ~~A-103을 조회만으로 둘 것인가~~ | **조회만 만들기로 했다 (2026-09-01). 만들었다** — `apps/admin` `/masters` |
| 5 | ~~관리팀 직원 상세에서도 계좌번호를 가릴 것인가~~ | **가리기로 정했다 (2026-09-07).** 모바일과 같은 `maskAccountNo`를 쓴다. 급여 이체에 원문이 필요해지면 **그 화면에서 따로 연다** — 직원 상세는 사람을 확인하는 자리지 이체하는 자리가 아니다 |

**다섯 다 정리됐다.** 5번은 9-07에 생겨 같은 날 정해졌다.
**서버를 기다리던 화면들은 2026-09-02 회신으로 전부 풀렸다** (1장).

2번을 A-501에 넣지 않은 이유 — 인벤토리가 A-501을 **조회**로 못박고 있고, 판정 재실행은
그 날 전 직원의 연장·야간·지각을 다시 매기는 동작이라 조회 화면에 곁들일 무게가 아니다.
기획도 같은 결론을 냈고 **새 화면을 만들었다.**

1번의 세 화면이 무엇으로 풀렸는지는 아래와 같다. 셋 다 API는 원래 다 있었고,
막고 있던 것은 "무엇을 보여줄지"였다.

| 화면 | 정해져야 하는 것 |
|---|---|
| ~~S-101 마이페이지~~ | **풀렸다 (2026-09-01).** 기본·소속입사·계좌·외국인 항목·내부 판정값을 보여주고 **개인 항목은 뺐다.** 조회는 만들었고, **통장정보 수정도 만들었다 (2026-09-07)** — 서버 10번 답(9-02)으로 풀린 것이다 |
| ~~S-501 내 근태 현황~~ | **풀렸다 (2026-09-01). 하루하루 다 보여주고 기본은 이번 달.** 만들었다. 52시간은 서버가 주 목록을 배열로 줘서 받은 만큼 그린다 — 물어볼 필요가 없었다 |
| ~~S-601 급여명세서~~ | **풀렸다 (2026-09-01). 확정된 것만 보여준다.** 만들었다. 진입에 추가 확인은 두지 않았다 — 정해지지 않은 것을 화면이 정하지 않는다 |

---

## 3. 인사팀

| # | 무엇 | 막히는 것 |
|---|---|---|
| 1 | ~~연차 발생 규칙~~ | **풀렸다 (2026-09-01). 입사일 기준 · 근로기준법 그대로** — 1년 미만은 개근한 달마다 1일(최대 11일), 1년차부터 15일, 3년차부터 2년마다 +1일(최대 25일). **서버 5번도 답이 와서(9-02) A-306은 이제 만들면 된다** — 신입휴가는 `NEW_HIRE`다. **다만 자동 부여 기준은 아래 B로 남았다** |
| 2 | 반차의 시작·종료 시각 | 9-01에 **오전 `09:00~13:00` · 오후 `13:00~18:00`, 점심 포함**으로 받아 S-301에 붙였다. **그런데 9-02에 직무 마스터(근무 `08:00~17:00`, 중식 `12:00~13:00`)와 어긋나는 것이 드러났다 — 아래 A** |

**9-01에 물었던 여섯은 다 답을 받았다.** 연차촉진은 **서버로 넘어갔고 9-02에 열렸다**
(서버 11번).

**그런데 9-02 서버 회신으로 둘이 새로 생겼다** — 아래 A·B다. 둘 다 값이 틀리면
46명의 연차·근태가 틀린 기준으로 계산되는 항목이라 **먼저 확인해야 한다.**

4·5·6은 지금 동작이 맞다는 답이라 고칠 것이 없었고, 1번은 값이 정해져 **A-306이 서버
5번 하나만 남았고**, 2번으로 **S-301 반차 신청을 붙였다.**

**남은 것은 A · B 둘이다.** 공휴일 입력은 2026년치를 넣어 끝났다 (아래).

**특별휴가(경조사) 일수표는 받지 않아도 된다.** 특별휴가는 발생(grant)이 아니라 신청이고,
연차를 깎지 않는다 (`deductLeave` 없음). 며칠인지는 서버·인사팀이 판단할 값이라
**앱이 알 필요가 없다.**

### 회사가 정해야 하는 것 (2026-09-02 서버 회신)

**서버와 우리 둘이 정할 수 없는 값이다.** 둘 다 인사팀 확인이 필요하다.

**A. 반차 시각이 직무 마스터와 어긋난다.**

| 출처 | 오전 | 오후 |
|---|---|---|
| 지금 시드 | `09:00~13:00` | `13:00~18:00` |
| 직무 마스터 | 근무 `08:00~17:00`, 중식 `12:00~13:00` | |

지금 값은 **앱이 갖고 있던 것을 그대로 옮긴 것**인데, 시드에 들어가는 순간 회사 공식
기준이 된다. 9-01에 인사팀에게 받은 값이 직무 마스터와 맞는지 확인해야 한다.
**필드는 바뀌지 않으니 화면은 먼저 만들어도 된다.**

**B. 연차 발생 기준이 두 갈래다.**

부여는 **입사일** 기준인데 잔여·소멸은 **회계연도**(1/1~12/31) 기준이다. 자동 부여를
켜려면 어느 쪽에 맞출지 정해야 한다. 그리고 **「매월 개근」을 무엇으로 판단할지**도
정해지지 않았다 — 결근 신청이 없으면 개근인지, 근태 기록까지 볼 것인지, 지각·조퇴가
개근을 깨는지.

**정해질 때까지 자동 부여는 꺼 둔 상태다.** 지금 켜면 46명의 연차가 틀린 기준으로 생긴다.

### ~~급한 것 — 공휴일 입력~~ — 2026년은 넣었다 (2026-09-09)

**연차 차감이 서버의 공휴일 목록을 본다.** 등록되지 않은 날은 근무일로 계산돼
**연휴가 연차에서 깎인다.** 그래서 확인이 아니라 입력이 필요한 항목이었다.

**2026년 21일이 됐다.** 시드에 15일이 있었고 **빠져 있던 다섯에 창립기념일까지 관리팀
화면에서 넣었다** (`POST /holidays` 여섯 건 201).

| 날짜 | 이름 |
|---|---|
| 2026-03-02 (월) | 삼일절 대체공휴일 |
| 2026-05-24 (일) | 부처님오신날 |
| 2026-05-25 (월) | 부처님오신날 대체공휴일 |
| 2026-07-06 (월) | **창립기념일** — 유일한 `회사 지정` |
| 2026-08-17 (월) | 광복절 대체공휴일 |
| 2026-10-05 (월) | 개천절 대체공휴일 |

**대체공휴일 규칙은 종류마다 다르다.** 이걸 몰라서 처음에 추석 대체공휴일(9-28)을
넣으려다 걸렀다 — 2026 추석 연휴는 목~토라 대체공휴일이 없다.

| 공휴일 | 대체공휴일이 붙는 조건 |
|---|---|
| 삼일절 · 광복절 · 개천절 · 한글날 · 부처님오신날 · 성탄절 | **토요일 또는 일요일**과 겹칠 때 |
| 설날·추석 연휴 | **일요일** 또는 다른 공휴일과 겹칠 때 (**토요일은 아니다**) |
| 어린이날 | 토요일 · 일요일 · 다른 공휴일과 겹칠 때 |
| 근로자의 날 · 현충일 · 신정 | **대체공휴일이 없다** |

**음력 날짜는 앱이 계산하지 않는다** (`CLAUDE.md` 3장). 2027년치는 다시 달력을 보고
넣는다 — 화면의 연도 선택에 2027년이 이미 있다.

**회사 지정은 창립기념일(7-06) 하나다.** 날짜는 사용자가 알려줬다. 다른 회사 지정 휴일이
있으면 인사팀에 확인해 넣는다 — `구분`을 `COMPANY`로 두는 것이 법정공휴일과의 차이다.

---

## 4. 이미 풀린 것 (다시 묻지 않는다)

| 무엇 | 언제 |
|---|---|
| **반차 시각 — 오전 `09:00~13:00` · 오후 `13:00~18:00`, 점심 포함** | 2026-09-01 인사팀 |
| **연차 발생 — 입사일 기준 · 근로기준법 그대로** (1년 미만 월 1일 최대 11일 / 1년차 15일 / 3년차부터 2년마다 +1일 최대 25일) | 2026-09-01 인사팀 |
| **A-502를 A-302와 나누지 않는다** — 회사 양식이 「휴가(근태)신청서」 한 장이다 | 2026-09-01 |
| **권한 체계 — 독립 역할이 아니고 관리팀 안에서 지정한다** | 2026-09-01 인사팀 |
| **주민등록번호는 관리팀 전원이 입력한다** | 2026-09-01 인사팀 |
| **지난 날짜 연차 신청(사후 신청)을 허용한다** | 2026-09-01 인사팀 |
| **단체연차 부족분은 명단만 받고 개별 처리한다** — 자동 무급 전환이 아니다 | 2026-09-01 인사팀 |
| 잔여는 `remaining`이다 (결재 대기까지 뺀 값) | 2026-08-31 |
| 전자서명 `signatureImage`는 base64, `CLICK`이면 안 보내도 된다 | 2026-08-31 |
| 재직증명서 단건·PDF 권한 구멍 — 구멍이 아니었다 (스텁 기본 사용자가 관리팀) | 2026-08-31 |
| `POST /certificates`는 201이 맞다 | 2026-08-31 |
| 400은 요청 형식, 422는 업무 규칙 위반 | 2026-08-31 |
| 근태 수집기 버그 셋 수정 배포 | 2026-08-31 |
| `expiresOn`을 비우면 그해 12월 31일 — **이월이 없다** | 2026-09-01 |
| **결재는 2단계다** — 검토(`/review`) 다음 승인(`/decision`). 상태에 `REVIEWED`가 있다 | 2026-09-02 서버 |
| **계좌는 마스킹 없이 원문으로 온다.** `PUT`은 셋을 전부 덮어쓴다 | 2026-09-02 서버 |
| **권한은 서버가 검증한다** — 남의 id로 부르면 거부된다 | 2026-09-02 서버 |
| `residentNoMasked` = `901231-1******` | 2026-09-02 서버 |
| `dayOfWeek`는 한글 한 글자 (`"월"` … `"일"`) | 2026-09-02 서버 |
| **서명 상한 base64 128KB · 배경은 투명 PNG** | 2026-09-02 서버 |
| `roundUnit`은 **절사**다 (반올림이 아니다) | 2026-09-02 서버 |
| **A-306 신입휴가는 `NEW_HIRE`** — `MONTHLY`가 아니다 | 2026-09-02 서버 |
| **근태 확정은 `confirm=false`로 되돌릴 수 있다** — 원래 됐다 | 2026-09-02 서버 |
| **`PUT /employees/{id}`에서 `residentNo`가 빠졌다** — 이름·연락처·주소를 이제 바꾼다. 원래도 안 실으면 보존됐다고 한다 | 2026-09-09 서버 |
| **계좌를 서버가 가려서 준다** — `bankAccount` → `bankAccountMasked`. 원문은 `GET /employees/{id}/bank-account`(이체용). 주민번호도 `GET /employees/{id}/resident-no`(`?full=true`는 신고 기능에서만) | 2026-09-09 서버 |
| **`GET /leave/promotions/me`** — 직원이 자기 촉진 통보를 본다. S-302로 들어가는 줄이 생겼다 | 2026-09-09 서버 |
| **`GET /leave/promotions`** — 관리팀이 남긴 기록을 다시 본다. `planSubmitted`로 2차 대상이 갈린다 | 2026-09-09 서버 |
| **권한이 Keycloak 롤 기준이 됐다** — 앱 DB가 아니라 토큰의 롤이 정본이다. 프런트가 바꿀 것은 없다 | 2026-09-09 서버 |
