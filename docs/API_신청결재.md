# 신청 · 결재 API 명세

Swagger `4. 신청 · 결재` 태그를 옮긴 것이다. 원본이 항상 우선한다.

| | |
|---|---|
| 출처 | `https://api.dvi-ind.com/hi-yo/v3/api-docs` (Swagger UI `/hi-yo/swagger-ui/index.html`) |
| API 버전 | `DVI 인사시스템 API` v1 |
| 확인일 | 2026-08-27 |
| 서버 | `https://api.dvi-ind.com/hi-yo` (개발) |

연차 · 반차 · 조퇴 · 외출 · 교육 · 결근을 **하나의 신청서**로 다룬다. 종류가 달라도
엔드포인트는 같고, 종류별로 달라지는 동작은 서버가 `GET /requests/types`로 알려준다.

> **결재에 전자서명이 필수다.** `POST /requests/{id}/decision`이 `signatureMethod`를 필수로 받는다.
> 그런데 **서명 이미지의 형식이 어디에도 정의돼 있지 않다** (7장 1번).
> A-302 승인·반려 화면은 이것이 풀려야 만들 수 있다.

> **신청 데이터가 0건이다.** 대기 목록도 내 신청 목록도 비어 있다 (2026-08-27).
> `LeaveRequestResponse`의 실제 응답을 아직 눈으로 보지 못했다. 화면을 붙이면 빈 상태부터 만난다.

---

## 1. 공통

### 인증 · CORS

급여와 같다. `docs/API_급여.md` 1장을 본다 — 개발용 스텁 헤더 `X-Debug-Employee-No`,
허용 오리진은 `http://localhost:5173` 하나뿐이다.

### 응답 형식

**급여와 다르다.** 목록이 배열이 아니라 봉투로 온다.

```json
{ "content": [], "page": 0, "size": 20, "totalElements": 0,
  "totalPages": 0, "first": true, "last": true }
```

`page`는 **0부터**다. `packages/api`의 `PageResponse<T>`가 이 모양이다.

- 날짜+시각에 타임존이 붙지 않는다. 서버는 한국 시간으로 돈다
- **연차 차감 일수는 서버가 계산한다.** 주말 · 공휴일은 세지 않는다.
  앱에서 다시 세거나 검산하지 않는다

### 오류

급여와 같은 모양이다.

```json
{ "timestamp": "2026-08-27T11:52:32.765332676", "status": 404,
  "error": "Not Found", "message": "신청서를 찾을 수 없습니다. id=1",
  "path": "/hi-yo/requests/1" }
```

`message`는 **사용자에게 그대로 보여줘도 되는 한국어**다. 앱에서 문구를 다시 만들지 않는다.

`422`는 값 오류가 아니라 **업무 규칙 위반**이다. API 소개문이 예로 든 것이 둘이다 —
**잔여 부족**, **검토 없이 승인**. 뒤엣것이 무엇을 뜻하는지는 7장 2번.

> 오류 응답이 스펙에 선언돼 있지 않다. **9개 엔드포인트 전부 200만 문서화돼 있다**
> (2026-09-02에 `/review`와 `/signature`가 늘어 아홉이 됐다).

---

## 2. 상태 흐름

**결재는 2단계다** (2026-09-02 서버 변경). 회사 「휴가(근태)신청서」(DV-MP-120-004)의
결재란이 「작성 · 검토 · 승인」 세 칸이고 **작성은 신청인 자리**라, 결재는 검토와 승인 둘이다.

```
신청 ──→ PENDING ──검토──→ REVIEWED ──승인──→ APPROVED
POST              POST /{id}/review     POST /{id}/decision
                       │                      │
                       └───── 반려 ──→ REJECTED ←─────┘

취소 (CANCELED)  DELETE /{id}
```

| 상태 | 뜻 |
|---|---|
| `PENDING` | 검토 대기 |
| **`REVIEWED`** | **검토를 마치고 승인을 기다린다** (2026-09-02에 생겼다) |
| `APPROVED` | 승인 |
| `REJECTED` | 반려 |
| `CANCELED` | 취소 |

- **검토에도 승인에도 전자서명이 필수다.** 종이 서식의 두 칸이 그대로 옮겨온 것이다
- **검토한 사람은 승인할 수 없다.** 같은 사람이 두 칸을 채우면 단계를 나눈 뜻이 없어
  서버가 **422**로 막는다. 화면에서 미리 비활성으로 둔다
- **검토 단계에서 반려하면 거기서 끝난다.** `approved: false`면 승인까지 갈 이유가 없다
- **`/decision`은 `REVIEWED`만 받는다.** 검토 전이면 그렇게 알려 준다
- 이미 승인·반려된 건은 **검토 칸이 비어 있다.** 실제로 검토한 사람이 없는데 이름을 넣으면
  그것이 증빙으로 남아 소급해 채우지 않았다. 결재 대기 중이던 건은 검토부터 다시 밟는다
- **취소는 대기중이면 언제든, 승인된 건은 시작 전에만** 된다
- **`REVIEWED`가 늘어난 것이 화면에 미치는 범위가 넓다.** 상태를 열거하는 곳
  — 목록 필터·뱃지·정렬 — 을 빠짐없이 고쳐야 한다

---

## 3. 엔드포인트

| 메서드 | 경로 | 권한 | 용도 |
|---|---|---|---|
| GET | `/requests` | 본인 | 내 신청 목록 |
| POST | `/requests` | **본인만** | 신청 등록 |
| GET | `/requests/{id}` | 본인 또는 관리팀 | 신청서 단건 |
| DELETE | `/requests/{id}` | 본인 | 취소 |
| PATCH | `/requests/{id}/signature` | **본인만** | 이미 낸 신청서에 신청인 서명을 붙인다 |
| POST | `/requests/{id}/review` | 관리팀 | **검토 — 결재의 첫 단계** |
| POST | `/requests/{id}/decision` | 관리팀 | **승인 / 반려 — 마지막 단계** |
| GET | `/requests/pending` | **관리팀** | 승인 대기 목록 |
| GET | `/requests/types` | — | 신청 종류 목록 |

목록 둘(`/requests`, `/requests/pending`)은 `page` · `size` · `sort` 쿼리를 받는다.

---

### GET /requests/types

**화면이 입력 폼을 그리려면 각 종류의 동작을 알아야 한다.** 종류별 규칙을 앱에 하드코딩하지 않는다.

**응답** `200` — `RequestTypeResponse[]`

**`halfDay`인 종류에는 반차 시각이 함께 온다** (2026-09-02부터). 서버 문서가
**「시각을 앱에 하드코딩하지 말고 이 값을 그대로 `POST /requests`에 실어 보내라」**고
적고 있다. 그전에는 앱이 값을 갖고 있어서 회사가 시각을 바꾸면 앱을 다시 배포해야 했다.

**지금 들어 있는 값은 확정이 아니다.** 앱이 갖고 있던 값을 그대로 옮겨 둔 것인데
직무 마스터(근무 `08:00~17:00`, 중식 `12:00~13:00`)와 어긋난다 —
인사팀 확인 대기 (`01_물어볼_것.md` 3장 A). **필드는 바뀌지 않는다.**

5장에 확인일 기준 실제 목록이 있다.

---

### POST /requests — 본인만

**요청** `LeaveRequestCreateRequest`
**응답** `200` — `LeaveRequestResponse`

차감 일수를 요청에 넣지 않는다. 서버가 계산해 `leaveDays`로 돌려준다.

**신청인 서명을 함께 받는다** (2026-09-02부터). `signatureMethod`·`signatureImage`이고
결재 서명과 형식이 같다. 응답의 **`applicantSigned`**가 신청인 서명 여부다 —
기존 `signed`는 **결재자** 서명이라 둘은 다른 값이다.

**서버에서는 선택이다.** 관리팀이 대리 등록하는 경로가 있어 필수로 두지 않았다.
**종이 서식을 그대로 옮기는 화면이면 화면에서 필수로 받는다.**

---

### GET /requests/{id}

신청서 단건. 상태를 바꾸지 않는다.

**`GET`이 검토가 아니다** (2026-09-02에 바뀌었다). 그전에는 이 호출을 읽어보는 것 자체가
요구사항의 「검토」라고 봤는데, **검토가 상태를 바꾸는 진짜 단계가 됐다** —
`POST /{id}/review`다.

**응답** `200` — `LeaveRequestResponse`
**오류** `404` — `신청서를 찾을 수 없습니다. id={id}`

---

### DELETE /requests/{id}

취소. **대기중이면 언제든, 승인된 건은 시작 전에만.**

**응답** `200` — `LeaveRequestResponse`

---

### PATCH /requests/{id}/signature — 본인만

**이미 접수된 신청서에 신청인 서명을 붙인다** (2026-09-02 신설).

**요청** `ApplicantSignatureRequest`
**응답** `200` — `LeaveRequestResponse`

**단체연차 때문에 생긴 경로다.** 관리팀이 적용을 누르는 순간 서버가 신청서를 대신 만들어
서명이 빠진다. 관리팀 대리 등록도 같다. **차감은 그대로 두고 서명만 나중에 채운다** —
전원의 서명을 기다리면 한 사람이 안 하면 급여 마감이 막히기 때문이다 (`API_연차.md` 8장).

| 규칙 | 이유 |
|---|---|
| 본인만 | 남의 신청서에 서명할 수 없다 |
| 다시 서명 가능 | 잘못 그렸을 때 고칠 방법이 없으면 안 된다. **옛 서명은 지운다** — 둘이 남으면 어느 것이 진짜인지 다투게 된다 |
| 결재 후 불가 | 결재자가 본 것과 다른 서명이 남으면 안 된다. **단체연차는 예외** — 처음부터 `APPROVED`라 개인 결재 절차가 없다 |
| 반려·취소 불가 | 이미 끝난 신청서다 |

---

### POST /requests/{id}/review — 관리팀

**검토 — 결재의 첫 단계** (2026-09-02 신설). **전자서명이 필수다.**

**요청** `RequestDecisionRequest` (`/decision`과 몸통이 같다)
**응답** `200` — `LeaveRequestResponse`

- 통과하면 상태가 **`REVIEWED`**가 되어 승인을 기다린다
- **`approved: false`면 검토 단계에서 반려되고 거기서 끝난다** — 승인까지 갈 이유가 없다
- 응답에 `reviewerId`·`reviewerName`·`reviewComment`·`reviewedAt`이 채워진다

---

### POST /requests/{id}/decision — 관리팀

**승인 또는 반려 — 결재의 마지막 단계.** **전자서명이 필수다.**

**요청** `RequestDecisionRequest`
**응답** `200` — `LeaveRequestResponse`

- `approved`가 `true`면 승인, `false`면 반려다
- **`REVIEWED`인 건만 받는다.** 검토 전이면 그렇게 알려 준다
- **검토한 사람은 승인할 수 없다** — 422. 종이 서식에서도 두 사람이 도장을 찍는다
- `signatureMethod`가 **필수**다. `IMAGE`와 `CLICK` 둘 중 하나.
  `CLICK`이면 이미지를 보내지 않아도 된다 (2026-08-31)
- `signatureImage`는 **base64 128KB**(`131,072`자)가 상한이다 (2026-09-02 확정)

---

### GET /requests/pending — 관리팀

승인 대기 목록. A-302가 여는 화면이다.

**응답** `200` — `PageResponseLeaveRequestResponse`

**`PENDING`과 `REVIEWED`가 섞여 온다.** 둘은 눌러야 할 사람이 다르므로
**목록에 상태 칸을 둔다** — 없으면 열어봐야만 지금 무엇을 해야 하는지 알 수 있다.

---

## 4. 스키마

### LeaveRequestResponse

| 필드 | 타입 | 설명 |
|---|---|---|
| `id` | int64 | |
| `employeeId` | int64 | |
| `employeeName` | string | |
| `departmentName` | string | |
| `typeCode` | string | 5장 종류 코드 |
| `typeName` | string | **표시명. 화면에 그대로 쓴다** |
| `startDate` / `endDate` | date | |
| `startTime` / `endTime` | string | 시각이 필요한 종류에만 |
| `leaveDays` | number | **서버가 계산한 차감 일수.** 앱에서 다시 세지 않는다 |
| `fiscalYear` | int32 | |
| `reason` | string | |
| `emergencyContact` | string | |
| `status` | `PENDING` / **`REVIEWED`** / `APPROVED` / `REJECTED` / `CANCELED` | |
| `companyLeave` | boolean | |
| `excludedDates` | `ExcludedDate[]` | 차감에서 빠진 날 |
| **`applicantSigned`** | boolean | **신청인** 서명 여부 (2026-09-02 신설) |
| **`reviewerId`** / **`reviewerName`** | | 검토한 사람. **이 사람은 승인할 수 없다** |
| **`reviewComment`** | string | |
| **`reviewedAt`** | date-time | |
| `approverId` / `approverName` | | **승인**한 사람 |
| `decisionComment` | string | |
| `signed` | boolean | **결재자** 서명 여부. `applicantSigned`와 다른 값이다 |
| `decidedAt` | date-time | |
| `deductibleMinutes` | int32 | |

`ExcludedDate` = `date`, `reason`

**`excludedDates`가 "왜 3일 신청인데 2일만 깎였나"에 답하는 값이다.** `reason`을 그대로 보여준다.
주말 · 공휴일 판정을 앱에서 하지 않는다.

`companyLeave` · `fiscalYear` · `signed` · `deductibleMinutes`의 정확한 뜻은 스펙에 설명이 없다
(7장 4번).

### LeaveRequestCreateRequest

| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `typeCode` | string | ✅ | 5장 종류 코드 |
| `startDate` | date | ✅ | |
| `endDate` | date | ✅ | |
| `startTime` | string | | `needTime`인 종류에만 |
| `endTime` | string | | |
| `reason` | string(≤255) | | |
| `emergencyContact` | string(≤50) | | |
| **`signatureMethod`** | `IMAGE` / `CLICK` | | 신청인 서명 (2026-09-02 신설) |
| **`signatureImage`** | string(≤131072) | | base64. `CLICK`이면 안 보내도 된다 |

### ApplicantSignatureRequest

`PATCH /requests/{id}/signature`가 받는다.

| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `signatureMethod` | `IMAGE` / `CLICK` | ✅ | |
| `signatureImage` | string(≤131072) | | base64 |

### RequestDecisionRequest

**`/review`와 `/decision`이 이것을 같이 쓴다.**

| 필드 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `approved` | boolean | ✅ | `true` 통과 / `false` 반려 |
| `comment` | string(≤255) | | |
| `signatureMethod` | `IMAGE` / `CLICK` | ✅ | |
| `signatureImage` | string(**≤131072**) | | base64. **128KB 상한이 스키마에 박혔다** (2026-09-02) |

### RequestTypeResponse

| 필드 | 타입 | 설명 |
|---|---|---|
| `id` | int64 | |
| `code` | string | |
| `name` | string | **표시명. 화면에 그대로 쓴다** |
| `deductLeave` | boolean | 연차에서 깎이는가 |
| `deductPay` | boolean | 급여에서 깎이는가 |
| `needTime` | boolean | 시작 · 종료 **시각**이 필요한가 |
| `halfDay` | boolean | 반차인가 |
| **`amStartTime`** / **`amEndTime`** | string | 오전 반차 시각. `09:00:00` 모양 (2026-09-02 신설) |
| **`pmStartTime`** / **`pmEndTime`** | string | 오후 반차 시각 |

**이 네 개의 boolean이 폼의 모양을 정한다.** 코드로 분기해 앱에서 규칙을 만들지 않는다.

**반차 시각도 앱에 하드코딩하지 않는다.** `halfDay`인 종류에 실려 오는 값을 그대로
`POST /requests`의 `startTime`·`endTime`에 넣는다. **안 실려 오면 지어내지 않는다** —
틀린 시각으로 신청서가 나가는 것보다 안 나가는 편이 낫다.

### PageResponseLeaveRequestResponse

`content`(`LeaveRequestResponse[]`), `page`, `size`, `totalElements`, `totalPages`, `first`, `last`

---

## 5. 신청 종류

**확인일 기준 실제 응답이다** (`GET /requests/types`, 11건).
표시명은 서버 `name`을 쓴다. 아래는 코드 목록일 뿐 화면 문구가 아니다.

| 코드 | 이름 | 연차 차감 | 급여 차감 | 시각 필요 | 반차 |
|---|---|---|---|---|---|
| `ANNUAL` | 연차휴가 | ✅ | | | |
| `HALF_DAY` | 반차휴가 | ✅ | | ✅ | ✅ |
| `OUTING` | 외출 | | | ✅ | |
| `EARLY_LEAVE` | 조퇴 | | | ✅ | |
| `SPECIAL` | 특별(경조사) | | | | |
| `UNPAID` | 무급휴무 | | ✅ | | |
| `RESERVE_TRAINING` | 예비군훈련 | | | | |
| `ABSENCE` | 결근 | | ✅ | | |
| `EDUCATION` | 교육 | | | | |
| `PAID_LEAVE` | 유급휴가 | | | | |
| `ETC` | 기타 | | | | |

- 연차를 깎는 것은 **연차휴가 · 반차휴가 둘뿐**이다
- 급여를 깎는 것은 **무급휴무 · 결근 둘**이다
- 시각을 받아야 하는 것은 **반차 · 외출 · 조퇴 셋**이다

**반차 시각이 확정됐다 (2026-09-01 인사팀)** — 오전 `09:00~13:00`, 오후 `13:00~18:00`,
**점심 포함**. `startTime`·`endTime`에 그대로 싣는다. 값은 `apps/mobile`의
`src/features/leave/halfDay.ts`에 있다.

**시각을 `RequestTypeResponse`에 넣어달라고 요청했다.** 지금은 `halfDay` 참·거짓만 오고
시각 필드가 없어서 앱이 값을 갖고 있다 — 회사가 시각을 바꾸면 앱을 다시 배포해야 한다.

**외출·조퇴 시각은 자유 입력이고 화면이 받는다.** `TimeField`를 만들었다 (2026-09-01).
분 단위를 끊지 않는다 — 종이 신청서도 시각 칸이 빈칸이다.

**이 표를 앱에 상수로 박지 않는다.** 종류가 늘거나 규칙이 바뀌면 서버만 고치면 되도록
`GET /requests/types`를 부른다. 위 표는 "지금 무엇이 있는가"를 사람이 읽으려고 옮긴 것이다.

### 종이 양식과 대조했다 (2026-09-01)

회사가 쓰는 「휴가(근태)신청서」 양식(`DV-MP-120-004 Rev.02`)의 휴가종류 칸이 **일곱 개**다.

```
□ 연차 휴가   □ 반차 휴가   □ 특 별(경조사)   □ 조퇴
□ 무급 휴무   □ 예비군 훈련   □ 기타 :
```

**API 코드와 하나씩 맞는다** — `ANNUAL` · `HALF_DAY` · `SPECIAL` · `EARLY_LEAVE` ·
`UNPAID` · `RESERVE_TRAINING` · `ETC`. 양식에 없는 것은 `OUTING`(외출) ·
`ABSENCE`(결근) · `EDUCATION`(교육) · `PAID_LEAVE`(유급휴가) 넷이다.

양식의 다른 칸도 API와 맞는다 — `총 연차`·`사용일수`·`잔여일수`(연차관리대장과 같은 값),
`비상연락처`(`emergencyContact`), `휴가기간 [날짜] [시] ~ [날짜] [시] ( N 일)`.
**반차·조퇴에 시각을 적는 칸이 종이에도 있다** — API가 `startTime`·`endTime`을 받는 것과 맞는다.


### 서명이 어디 들어가는가 (2026-09-02, 양식 원본을 받았다)

**신청서에는 신청인 서명란이 따로 없다.** 대신 오른쪽 위에 결재란이 있고 칸이 셋이다.

```
┌─────┬───────┬───────┬───────┐
│ 결  │ 작 성 │ 검 토 │ 승 인 │
│ 재  │       │       │       │   ← 도장·서명 자리 셋
└─────┴───────┴───────┴───────┘
```

- **`작성`이 신청인 자리로 보인다.** 종이에서 신청인이 여기 도장을 찍는 것이 보통이지만
  **확인이 필요하다** — 확인 전까지 화면에 그렇게 적지 않는다
- **`검토`·`승인`이 두 칸이다.** 인사팀은 2026-09-01에 「검토자·승인자가 독립 역할이 아니라
  관리팀 안에서 지정」이라고 답했는데, 종이는 두 사람이 찍는 모양이다.
  **어긋난다** — 7장 확인 목록에 올렸다
- 지금 API는 결재 서명 하나뿐이다 (`RequestDecisionRequest`의 `signatureMethod`).
  종이의 세 칸 중 무엇에 대응하는지 정해져 있지 않다

**연차사용계획서에는 서명란이 명시돼 있다** — 「제출자 : ______ (서명 또는 인)」.
그쪽은 `API_연차.md` 10장에 적었다.

### 실제로 적힌 신청서를 봤다

받은 파일에 기재 예시가 들어 있었다 — 소속 `생산관리`, 직급 `책임`, 휴가사유 `병원`,
휴가기간에 `14-15 시`처럼 **시각이 손으로 적혀 있다.** 우리가 `TimeField`를 만들어
분 단위를 끊지 않은 것과 맞는다.

**총 연차 · 사용일수 · 잔여일수 칸은 비어 있었다** — 신청자가 적는 자리이고,
그 값을 화면이 채워주면 종이보다 나아지는 지점이다.
---

## 6. 앱 관점 정리

### S-301 연차 현황 및 신청 (`apps/mobile`)

| 단계 | 호출 |
|---|---|
| 종류 목록 | `GET /requests/types` → 폼의 모양과 **반차 시각**을 정한다 |
| 신청 | `POST /requests` — 신청인 서명을 같이 싣는다 |
| 나중에 서명 | `PATCH /requests/{id}/signature` — 단체연차·대리 등록 건 |
| 내 신청 목록 | `GET /requests` |
| 취소 | `DELETE /requests/{id}` |

- 잔여 초과는 **제출 시 오류 반환**으로 간다 (`00_문서_인덱스.md` 확정). 버튼은 항상 눌린다
- 잔여 일수는 이 태그에 없다. `6. 연차` 태그이며 **아직 문서화되지 않았다**
- 차감 일수를 앱에서 세지 않는다. 서버가 준 `leaveDays`와 `excludedDates`를 보여준다

### A-302 연차 신청 검토 · 승인 (`apps/admin`)

| 단계 | 호출 |
|---|---|
| 대기 목록 | `GET /requests/pending` — `PENDING`과 `REVIEWED`가 섞여 온다 |
| 내용 보기 | `GET /requests/{id}` |
| **검토** | `POST /requests/{id}/review` — 첫 단계 |
| **승인 · 반려** | `POST /requests/{id}/decision` — 마지막 단계 |

- **만들었다** (2026-09-02). 단계마다 버튼이 갈려서 동작을 `features/approvals/ApprovalActions.tsx`로 떼어냈다
- **검토자에게는 승인 버튼이 비활성이다.** 서버가 422로 막지만 화면이 미리 막는다 —
  누르고 나서 거부당하게 두는 것은 화면의 잘못이다
- **목록에 상태 칸을 둔다.** 두 단계가 섞여 오고 눌러야 할 사람이 다르다
- 반려는 되돌릴 수 없다 — `danger` 버튼 + 확인 대화상자 (`DESIGN_ADMIN.md` 5 · 6장)
- 대기 목록은 봉투로 오고 페이지네이션이 있다. 표 정렬은 **서버가 지원하는 열에만** 넣는다
- **본인용 화면에서 `GET /requests/pending`을 부르지 않는다.** `apps/mobile`에 넣지 않는다

### A-502 신청형 근태 승인 — 나누지 않는다 (2026-09-01 확정)

화면 인벤토리는 A-302(연차 신청 검토·승인)와 A-502(신청형 근태 승인)를 **다른 화면**으로
두고 있다. 그런데 서버는 둘을 나눌 재료를 주지 않는다.

- `GET /requests/pending`의 파라미터는 `pageable` **하나뿐**이다. 종류로 거르는 값이 없다
- `RequestTypeResponse`에 모듈 구분이 없다. `deductLeave`·`deductPay`·`needTime`·
  `halfDay` 네 개는 **폼의 모양**을 정하는 값이지 "이건 연차, 이건 근태"가 아니다
- 11개 종류를 앱이 두 무리로 갈라야 하는데 그 기준이 문서 어디에도 없다.
  외출·조퇴는 근태처럼 보이고 특별(경조사)·교육·유급휴가는 어느 쪽인지 알 수 없다

**지금 A-302 화면이 대기 목록 전부를 보여준다.** 종류 칸에 서버가 준 `typeName`이 그대로
나오므로 외출·조퇴·교육도 여기서 결재된다 — 기능이 빠진 것이 아니라 **한 화면에 있다.**

나누려면 둘 중 하나가 필요하다.

1. `GET /requests/pending`에 종류 필터를 열어 준다, 또는
2. `RequestTypeResponse`에 모듈 구분(연차 / 근태)을 붙여 준다

**둘 다 없으면 나누지 않는다.** 앱이 11개를 임의로 가르면 어느 쪽에도 안 나오는 종류가
생기고, 그 신청은 아무도 결재하지 않은 채 남는다 (`CLAUDE.md` 3장).

**종이 양식이 답을 줬다 — 나누지 않는 것이 맞다** (2026-09-01).
회사 양식의 제목이 「**휴가(근태)신청서**」이고, 연차·반차와 조퇴·무급휴무·예비군이
**한 장에 같이 있다.** 회사가 원래 휴가와 근태를 한 장으로 신청하고 있었다.
나누는 것이 종이 절차와 어긋나므로 **서버에 종류 필터를 요청하지 않는다.**
A-302 한 화면으로 두고, 인벤토리의 A-502는 그 화면이 겸한다.

---

## 7. 미확정 — 확인이 필요한 것

**2026-09-02 회신으로 다섯이 풀렸다** (1 · 2 · 5 · 10 · 11). 그중 둘은 **뒤집혔다** —
5번과 11번은 「2단계 결재는 없다」로 정리해 뒀는데 종이 서식이 답을 바꿨다.

### 풀린 것

| # | 항목 | 답 |
|---|---|---|
| 1 | 전자서명 형식·크기 | **풀렸다.** base64 **128KB**(`131,072`자), 배경은 **투명 PNG**. 상한이 스키마에 박혔다 (2026-09-02). **물어본 것이 사고를 막았다** — 저장 컬럼이 `TEXT`(64KB)라 실측 최악값 72KB도 안 들어갔다. `MEDIUMTEXT`로 넓혔다 |
| 2 | 「검토 없이 승인」 422 | **풀렸다.** `GET /{id}`가 검토로 집계되는 것이 아니었다 — **검토가 상태를 바꾸는 진짜 단계**가 됐다(`POST /{id}/review`). 422는 ① `REVIEWED`가 아닌 건에 `/decision`을 부를 때 ② **검토한 사람이 승인하려 할 때** 난다 |
| 5 | 결재 단계 | **뒤집혔다.** 2026-09-01에 「검토자·승인자가 독립 역할이 아니라 관리팀 안에서 지정」이라 정리하고 2단계 결재가 없는 것으로 봤는데, **회사 서식을 받고 나서 단계 자체가 둘로 나뉘었다.** 인사팀이 「원칙대로 가자」고 확정했다 |
| 10 | 신청서에 신청인 서명 자리가 없다 | **풀렸다.** `POST /requests`가 `signatureMethod`·`signatureImage`를 받고 응답에 **`applicantSigned`**가 온다. 이미 낸 신청서에는 `PATCH /{id}/signature`로 나중에 붙인다 |
| 11 | 결재란이 세 칸인데 API는 한 번이다 | **풀렸다. 두 사람이 찍는 것이 맞았다.** 「작성」은 신청인 자리이고 「검토」·「승인」이 결재 둘이다 |

### 남은 것

| # | 항목 | 왜 문제인가 |
|---|---|---|
| 3 | **오류 응답 미문서화** | **9개 전부 200만 선언돼 있다.** 401/403/404/422가 어떤 상황에 오는지 목록이 필요하다. 다만 `docs/frontend-422-catalog.md`를 받아 **422는 정리됐다** (2026-09-02) |
| 4 | **필드 뜻** | `companyLeave` · `fiscalYear` · `signed` · `deductibleMinutes`에 설명이 없다. **`signed`는 풀렸다** — `applicantSigned`가 생기면서 결재자 서명임이 분명해졌다. **`companyLeave`는 짐작이 선다** — 단체연차 `apply`가 만든 신청서 표시로 보인다 (`API_연차.md` 8장). 맞는지만 확인하면 화면에 「회사가 정한 날」이라고 적을 수 있다 |
| 6 | **관리팀의 직원별 조회** | `GET /requests`는 내 것만 준다. 관리팀이 **특정 직원의 신청 이력**을 보는 API가 없다. 대기 목록만 있다 |
| 7 | **취소 권한** | `DELETE /{id}`를 관리팀도 부를 수 있는지, 본인만인지 적혀 있지 않다. 스펙 설명은 「대기중이면 언제든, 승인 건은 시작 전에만」까지다 |
| 9 | **데이터 없음** | 신청이 0건이라 `LeaveRequestResponse` · `excludedDates`의 실제 응답을 아직 못 봤다. **로그인이 열려 이제 확인할 수 있다** (2026-09-03) |
| 12 | **검토자가 반려도 막히는가** | 서버 설명이 「검토한 사람은 **승인**할 수 없다」까지다. 화면은 지금 **승인만 비활성**으로 두고 반려는 눌리게 뒀다. 막히는 것이 맞으면 반려도 같이 비활성으로 바꾼다 (2026-09-04) |

**8번(연차 잔여)은 지웠다** — `6. 연차` 태그가 `API_연차.md`로 정리됐다.

**A-302를 막던 것이 없어졌다.** 1번(서명)과 2번(422)이 그것이었고 둘 다 풀렸다.
**화면은 만들었다** (2026-09-02 · 6장). 남은 것은 화면을 만들면서 채울 수 있는 것들이다.

---

## 8. 전자서명 (2026-08-31)

### 형식은 확정됐다

서버 답변 — `signatureImage`는 **base64 문자열**이다. data URL이 아니다.
`CLICK`일 때는 **보내지 않아도 된다.**

### 크기 상한 — 실측값을 넘겼다

서버가 "프런트에서 실제로 얼마나 나오는지 알려주면 그 값으로 잡겠다"고 해서 브라우저에서
캔버스로 직접 쟀다 (2026-08-31). 라이브러리 없이 `canvas.toDataURL('image/png')`이다.

**캔버스 340×160 CSS px, 백킹스토어 680×320 (DPR 2 고정)**

| 획 수 | PNG (투명) | base64 |
|---|---|---|
| 2획 | 18 KB | 24 KB |
| **3획 — 보통 서명** | **23 KB** | **31 KB** |
| 5획 | 27 KB | 36 KB |
| 10획 | 39 KB | 52 KB |
| 20획 | 51 KB | 68 KB |
| **40획 — 거의 낙서** | **54 KB** | **72 KB** |
| 80획 | 42 KB | 55 KB |

**획을 아무리 늘려도 55 KB 근처에서 멈춘다.** 선이 겹치기 시작하면 오히려 압축이 잘 돼서
줄어든다. 전면을 검게 칠하면 5.6 KB다 — PNG는 단색 면을 거의 공짜로 압축한다.

### 캔버스 크기를 고정하지 않으면 터진다

| 캔버스 | PNG |
|---|---|
| 340×160 @2x | 23 KB |
| 340×160 @3x | 44 KB |
| 600×240 @3x, 빽빽하게 | **204 KB** |
| 800×300 @3x, 빽빽하게 | **373 KB** |

**백킹스토어를 DPR 2로 고정한다.** 요즘 폰 상당수가 DPR 3이라 기기 값을 그대로 쓰면
한 장에 수백 KB가 된다. 서명은 선 몇 획이라 2배로 충분하다.

### 제안한 것 — **둘 다 그대로 확정됐다** (2026-09-02)

- 서버 상한 **base64 기준 128 KB** — 실측 최악값의 약 1.8배다. 정상 서명이 걸릴 일은 없고,
  실수로 큰 캔버스를 보내는 것은 막힌다
- 배경은 **투명**. 흰 배경보다 10% 작고 문서에 얹을 때 유리하다

**둘 다 제안한 값 그대로 왔다.** 상한은 `131,072`자로 스키마에 박혔고
(`RequestDecisionRequest`·`ApplicantSignatureRequest`), 배경은 **서명이 출력 문서 서식
위에 얹히기 때문에** 투명이 맞다는 답이다 — 흰 배경이면 밑의 서식 선을 가린다.

**물어본 것이 사고 하나를 막았다.** 저장 컬럼이 `TEXT`(64KB)여서 128 KB는커녕
**위 실측표의 최악값 72 KB도 들어가지 않았다.** 길게 그은 서명 하나가 신청을 통째로
실패시키는 상태였고, `MEDIUMTEXT`로 넓혔다.

### 서명칸을 만들었다 (2026-09-02)

관리팀 결재 화면(`A-302`)의 승인·반려 대화상자에 넣었다 —
`apps/admin/src/components/SignaturePad.tsx`. 라이브러리를 쓰지 않는다.

- **그리면 `IMAGE`, 비워두면 `CLICK`.** 서버가 `CLICK` 일 때는 이미지를 받지 않아도
  된다고 했으므로 지금 동작을 깨지 않는다
- **캔버스 340×160 CSS px, 백킹스토어 680×320 고정.** 위 실측표의 그 크기다
- 보내는 것은 **base64 문자열**이다. `data:image/png;base64,` 앞머리를 뗀다
- **128 KB 를 넘으면 보내지 않고 다시 그리라고 한다.** **서버 상한과 같은 값으로
  확정됐다** (2026-09-02) — 고칠 것이 없었다
- 배경은 **투명**이다. **이것도 그대로 확정됐다**

실제로 눌러 확인했다 (2026-09-02) — 열두 획을 그리고 보낸 몸통이
`{ approved, signatureMethod: "IMAGE", signatureImage }` 이고 **12.2 KB**,
PNG 헤더가 맞고 `data:` 앞머리가 없다. 아무것도 안 그리고 반려하면
`{ approved, signatureMethod: "CLICK" }` 만 간다.

### 모바일 서명칸도 만들었다 (2026-09-02)

`apps/mobile/src/components/SignaturePad.tsx`. React Native 에는 캔버스가 없어서
그리는 것도 base64 로 굽는 것도 라이브러리가 필요했는데, **`react-native-webview` 하나로
정하고 관리팀에 만들어 둔 캔버스 코드를 그대로 넣었다** — 후보 넷을 비교한 기록은
`02_모바일_서명_라이브러리.md`에 있다. 크기 상한·백킹스토어 2배 고정·`data:` 앞머리
떼기까지 관리팀과 결과가 같다.

**아직 신청 화면에 붙이지는 않았다.** `POST /requests`가 신청인 서명을 받게 됐지만
(2026-09-02) `LeaveRequestInput`에 아직 자리가 없다. **붙이는 것이 다음 일이다** —
종이 서식의 「작성」 칸이라 화면에서는 필수로 받는다.
