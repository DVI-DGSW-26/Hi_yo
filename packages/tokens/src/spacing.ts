/**
 * 여백과 크기.
 *
 * **여러 화면·컴포넌트에서 반복되는 값만 여기 둔다.** 한 곳에서만 쓰는 숫자는
 * 그 컴포넌트 안에 두고, 두 번째로 쓰이는 순간 올린다. 전부 토큰으로 만들면
 * 이름을 찾는 비용이 값을 고정하는 이득보다 커진다.
 */
export const spacing = {
  /** 화면 좌우 패딩 */
  screenX: 24,
  /** 섹션 상하 패딩 */
  sectionY: 28,
  /** 섹션 제목 아래 여백 */
  sectionTitleGap: 20,
  /** 리스트 항목 사이 */
  rowGap: 18,
  /** 붙어 있는 두 요소 사이 — 라벨↔입력칸, 값↔보조 문구, 오류 문구↔필드 */
  tight: 8,
  /** 하단 CTA 영역 좌우 패딩 */
  ctaX: 20,
  /** 상단 네비게이션 높이 */
  navHeight: 52,
  /** 관리팀 화면 좌측 메뉴 폭 */
  sideNavWidth: 200,
  /**
   * 한 줄의 최소 높이. 모바일 리스트 행과 관리팀 표 행이 같은 값을 쓴다.
   * 최소 터치 영역 44×44에서 온 값이라 줄이지 않는다. 고정 높이로 쓰지 않는다 —
   * 글꼴을 키우면 이보다 커져야 한다.
   */
  rowHeight: 44,
  /** 섹션 구분 띠 높이 */
  dividerHeight: 10,

  /*
   * 아래 다섯은 관리팀 화면(웹)의 값이다. 모바일은 쓰지 않는다.
   * 근거는 `docs/DESIGN_ADMIN.md` 11장에 있다.
   */

  /** 본문 좌우 패딩. 화면 좌우(`screenX` 24)보다 넓다 — 좌측 메뉴와 표 사이가 붙으면 답답하다 */
  mainPadX: 32,
  /** 본문 상하 패딩 */
  mainPadY: 36,
  /** 화면 안 블록 사이 — 제목 ↔ 요약 ↔ 표 */
  blockGap: 24,
  /**
   * 읽는 문장의 최대 너비. **표에는 주지 않는다.**
   *
   * 한 줄이 화면 폭만큼 길면 눈이 다음 줄 첫 글자를 못 찾는다. 그래서 설명·안내문에만
   * 건다 — 본문 전체에 최대 너비를 두지 않는다는 규칙(`DESIGN_ADMIN.md` 2장)은 표 이야기다.
   */
  readWidth: 620,
  /**
   * 표 행의 최소 높이.
   *
   * 48에서 44로 내렸다 (2026-09-02). 관리팀 화면은 마우스로 쓰는 표라 손가락 여유가
   * 필요 없고, 한 화면에 마흔 줄이 넘게 오는 표에서 그 여유가 헐렁함으로 읽혔다.
   *
   * **44 아래로는 내리지 않는다.** `rowHeight`(44)가 최소 터치 영역에서 온 바닥이고,
   * 목록의 행은 눌러서 상세로 가는 자리다.
   */
  tableRowHeight: 44,
} as const;

export const radius = {
  button: 14,
  chip: 8,
} as const;
