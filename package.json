{
  "name": "hr-system",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "mobile": "npm run start --workspace @hr/mobile",
    "admin": "npm run dev --workspace @hr/admin"
  }
}
