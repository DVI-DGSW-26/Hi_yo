# HR 시스템 — AI 작업 지침

## 시작 전 반드시 읽을 것

1. `docs/DESIGN_RULES.md` — UI 코드를 한 줄이라도 쓰기 전에 읽는다. 이 파일보다 우선한다.
2. `docs/DESIGN_SYSTEM.md` — 컬러·폰트 크기·여백의 정확한 값과 컴포넌트 사용법.
3. `docs/00_문서_인덱스.md` — 어떤 정보가 확정됐고 어떤 것이 비어 있는지 목록이다.

`docs/` 안의 문서에서 `TODO`로 남아 있는 항목은 **아직 정해지지 않은 것**이다.
그 부분을 추측해서 구현하지 않는다. 멈추고 사람에게 알린다.

## 프로젝트 구조

```
apps/mobile     Expo (React Native). 본인용 화면 전부
apps/admin      React + Vite. 관리팀 화면 (테이블 6컬럼 이상)
packages/tokens 컬러/타이포/여백 토큰. 양쪽이 공유
packages/api    HTTP 클라이언트·오류 정규화·목록 봉투 타입. 양쪽이 공유
```

`packages/api`에는 **플랫폼과 무관한 것만** 둔다. 인증과 baseURL은 앱이 넣는다 —
모바일은 `expo-secure-store`·`EXPO_PUBLIC_`, 관리팀 화면은 브라우저·`VITE_`로 사정이 다르다.
서버 오류 규칙(422는 업무 규칙 위반 등)이 바뀌면 여기 한 곳만 고친다.

`apps/admin`은 라우팅에 **react-router**를 쓴다. 토큰은 `src/lib/applyTokens.ts`가
CSS 변수로 심는다. CSS에 hex나 여백 숫자를 직접 적지 않는다.

---

## 1. 절대 규칙

1. **색상 hex 값을 컴포넌트에 직접 쓰지 않는다.** `@hr/tokens`의 `colors`만 쓴다.
   토큰에 없는 색이 필요하면 코드를 쓰지 말고 사람에게 묻는다.
2. **여백 숫자를 임의로 정하지 않는다.** `spacing` 토큰을 쓴다.
3. **새 UI 컴포넌트를 즉석에서 만들지 않는다.** `src/components`의 기존 컴포넌트를 조합한다.
   없으면 만들기 전에 사람에게 묻는다.
4. **모든 사용자 대상 문구는 해요체.** "신청되었습니다" (X) → "신청했어요" (O)
5. **그림자(shadow/elevation), 그라디언트를 쓰지 않는다.**
6. **한 화면에 Primary 버튼은 1개, 그린이 등장하는 지점은 2곳 이하.**

---

## 2. 개인정보와 보안 (가장 중요)

이 앱은 전 직원의 급여·주민번호·근태 기록을 다룬다. 실수가 곧 사고다.

### 절대 금지

- **민감정보를 로그에 남기지 않는다.** 급여액, 주민번호, 계좌번호, 인증 토큰을
  `console.log`, `Sentry`, 에러 메시지, 크래시 리포트에 넣지 않는다.
  디버깅용 `console.log`도 커밋하지 않는다.
- **토큰을 `AsyncStorage`에 저장하지 않는다.** `expo-secure-store`만 쓴다.
- **민감정보를 URL 쿼리스트링에 넣지 않는다.** 서버 로그에 남는다.
- **`EXPO_PUBLIC_` 환경변수에 시크릿을 넣지 않는다.** 앱 번들에 평문으로 들어간다.
- 개인정보를 로컬에 캐시할 때 만료 없이 두지 않는다.

### 마스킹

화면에 그대로 노출하지 않고 아래 형태로 보여준다.

| 항목 | 표시 |
|---|---|
| 주민번호 | `901231-1******` |
| 계좌번호 | `국민 ****-**-**1234` |
| 연락처 | `010-2847-****` |
| 이메일 | `min***@company.com` |

마스킹은 **서버에서 마스킹된 값을 받는 것이 원칙**이다. 클라이언트에서 원본을 받아 가리기만 하면
개발자 도구나 네트워크 로그로 그대로 보인다. 서버가 원본을 주고 있으면 코드를 쓰지 말고 알린다.

### 권한

- 모든 화면은 **본인용**과 **관리팀용**이 명확히 구분된다.
- 본인용 화면에서 다른 직원의 데이터를 조회하는 API를 호출하지 않는다.
- 관리팀 화면 코드를 `apps/mobile`에 넣지 않는다.
- 권한 체크를 클라이언트에서만 하지 않는다. 서버 검증이 없으면 알린다.

---

## 3. 도메인 로직 — 추측 금지

**HR 규칙은 회사마다 다르다. 모르면 지어내지 말고 물어본다.**

AI가 그럴듯하게 만들어내기 쉬운 것들이다. 아래 항목의 계산 로직을 구현해야 하는데
`docs/`에 확정된 규칙이 없으면 **코드를 쓰지 말고 사람에게 묻는다.**

- 연차 발생·소멸 기준 (입사일 기준인가 회계연도 기준인가)
- 연차 차감 단위 (반차·반반차 허용 여부)
- 주 52시간 산정 기준 (주 시작 요일, 연장근로 포함 범위)
- 4대보험 요율, 소득세 간이세액표
- 급여 일할계산 방식
- 당직·휴일근로 수당 배율
- 지각·조퇴 처리 기준

**계산은 전부 서버가 한다 (확정).** 급여·연차 잔여일수·근무시간 합계·52시간 비율은
서버가 계산한 값을 받아서 표시만 한다. 앱에서 다시 계산하거나 검산하거나 보정하는 코드를
작성하면 규칙 위반이다. 서버 응답에 필요한 값이 없으면 앱에서 만들지 말고 알린다.

---

## 4. API와 데이터

- **없는 엔드포인트를 지어내지 않는다.** API 명세에 없는 경로를 호출하는 코드를 쓰지 말고,
  필요한 엔드포인트를 사람에게 알린다.
- **목(mock) 데이터를 실제 코드에 남기지 않는다.** 필요하면 `src/mocks/`에 두고 명확히 분리한다.
  하드코딩된 이름·금액이 화면 컴포넌트 안에 남아 있으면 안 된다.
- 서버 상태는 **TanStack Query**로만 다룬다. 서버 데이터를 `zustand`에 복사해 넣지 않는다.
- 모든 쿼리는 **로딩·에러·빈 상태 세 가지를 반드시 구현한다.** 성공 케이스만 만들지 않는다.
- HTTP는 `src/lib/api.ts`의 axios 인스턴스 하나만 쓴다. `fetch`를 직접 쓰지 않는다.

---

## 5. 날짜·숫자 처리

- 서버는 UTC, 화면은 **KST 고정**. 기기 타임존을 따라가지 않는다.
  근태 기록이 해외 출장 중에 어긋나면 사고다.
- 날짜 라이브러리는 `date-fns` 하나만 쓴다. `moment`를 추가하지 않는다.
- 금액은 세 자리 콤마, 소수점 없음. 원 단위 절사 규칙은 서버를 따른다.
- 근무시간은 `8시간 20분` 형태로 표기한다. `8.33시간` 같은 소수 표기를 쓰지 않는다.
- 화면에 나가는 모든 숫자는 포맷 함수(`src/lib/format.ts`)를 거친다. 컴포넌트에서 직접 계산하지 않는다.

---

## 6. 기기 대응과 접근성

전 직원이 쓰는 앱이다. 개발자 기기에서만 잘 나오면 안 된다.

- **시스템 글꼴 확대 설정을 켠 사용자가 있다.** 고정 높이 컨테이너 안에 텍스트를 가두지 않는다.
  글자가 커져도 레이아웃이 깨지지 않아야 한다.
- 작은 화면(iPhone SE, 375×667)에서 하단 CTA가 가려지지 않는지 확인한다.
- `SafeAreaView` 또는 `useSafeAreaInsets`를 쓴다. 상하단 여백을 상수로 박지 않는다.
- 터치 영역은 최소 44×44. 아이콘 버튼은 `hitSlop`을 준다.
- 아이콘만 있는 버튼에 `accessibilityLabel`을 붙인다.

---

## 7. 스택 (변경 금지)

- 라우팅: expo-router (파일 기반)
- 서버 상태: TanStack Query
- 전역 상태: zustand (인증, 사용자 프로필 정도만)
- HTTP: axios
- 폼: react-hook-form + zod
- 날짜: date-fns
- 스타일: StyleSheet + 토큰

**라이브러리를 임의로 추가하지 않는다.** 필요하면 이유와 함께 사람에게 먼저 제안한다.
특히 NativeWind, styled-components, UI 킷(react-native-paper 등)을 추가하지 않는다.
디자인 규칙과 충돌한다.

---

## 8. 코드 작성 방식

- 새 화면을 만들 때는 먼저 **"이 화면이 전달할 단 하나의 메시지"**를 한 문장으로 적고 시작한다.
- 명세서의 필드를 순서대로 나열하는 방식으로 화면을 구성하지 않는다.
- 파일 구조: `app/`(라우트) / `src/components/`(공통) / `src/features/{모듈}/`(화면별 로직)
- 컴포넌트 파일 하나에 컴포넌트 하나. 200줄을 넘으면 분리한다.
- 요청받지 않은 리팩터링을 하지 않는다. 특히 여러 파일을 한 번에 고치지 않는다.
- 기존 테스트를 지우거나 `skip` 처리하지 않는다.
- 커밋 메시지는 한국어, `feat:` `fix:` `chore:` 접두사를 붙인다.

---

## 9. 막혔을 때

아래 상황에서는 **추측해서 코드를 쓰지 말고 멈추고 묻는다.**

- 도메인 규칙(3장)이 문서에 없을 때
- 필요한 API 엔드포인트가 명세에 없을 때
- 디자인 규칙과 요청이 충돌할 때
- 토큰에 없는 색·여백이 필요해 보일 때
- 새 라이브러리가 필요해 보일 때

그럴듯하게 만들어낸 코드보다, 무엇이 없는지 알려주는 편이 훨씬 낫다.

---

## 10. 커밋 전 확인

`docs/DESIGN_RULES.md` 9장 체크리스트에 더해 아래를 확인한다.

- [ ] `console.log`가 남아 있지 않은가
- [ ] 민감정보가 로그·에러 메시지에 노출되지 않는가
- [ ] 로딩·에러·빈 상태를 구현했는가
- [ ] 하드코딩된 목 데이터가 화면 컴포넌트에 남아 있지 않은가
- [ ] 글꼴 크기를 키운 상태에서 레이아웃이 깨지지 않는가
